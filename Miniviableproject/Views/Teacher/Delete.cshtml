@model Miniviableproject.Models.Teacher

<h2>Delete Teacher</h2>
<p>Are you sure you want to delete this teacher?</p>
<div>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.teacherfname)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.teacherfname)
        </dd>
        
    </dl>
</div>

@using (Html.BeginForm("DeleteConfirmed", "Teacher", new { id = Model.teacherid }, FormMethod.Post, new { onsubmit = "return confirm('Are you sure you want to delete this teacher?');" }))
{
    <input type="submit" value="Delete" class="btn btn-danger" />
}

<a href="@Url.Action("List", "Teacher")">Back to List</a>
